# Статус реализации

Обновлено: 23 сентября 2026 года.

Выполнены этапы 1–3. В этом продолжении проверен этап 2 и реализован только
один следующий этап — импорт IEK и архивного прайса. Этапы 4–12 не начаты.
Задания прочитаны из файла «Промпты_Codex_HackAlem.md»; обратные слеши перед
подчёркиваниями в сообщении пользователя трактовались как Markdown-экранирование.

## Очередь

- [x] 1. Подготовить проект и закрепить требования.
- [x] 2. Реализовать импорт Systeme Electric и версии данных.
- [x] 3. Реализовать импорт IEK и архивного прайса.
- [ ] 4. Согласовать источники и реализовать проверки качества.
- [ ] 5. Создать воспроизводимый проверочный набор.
- [ ] 6. Выделить разовые сделки и обработать возвраты.
- [ ] 7. Реализовать оценку упущенного спроса.
- [ ] 8. Реализовать прогноз сезонности и роста.
- [ ] 9. Реализовать расчёт пополнения и объяснения.
- [ ] 10. Создать интерфейс закупщика.
- [ ] 11. Добавить корректировки, утверждение и экспорт.
- [ ] 12. Проверить весь кейс и подготовить демонстрацию.

## Результат этапа 1

- Проверены PDF кейса, аналитический отчёт, состояние Git и отсутствие
  существующего приложения. До изменений отслеживался только шаблонный README,
  изменений в отслеживаемых файлах не было. Исходники PDF/XLSX оставлены на месте.
- Созданы `AGENTS.md`, `docs/SPEC.md` и этот статус с пятью требованиями кейса,
  ограничениями данных и границами этапов.
- Подготовлены `pyproject.toml`, `uv.lock` и локальное окружение `.venv`.
- Создан стартовый экран «Помощник закупщика» с честным пустым состоянием.
  Экран не импортирует отчёты, не имитирует прогнозы и не создаёт заказы.
- UI находится в `hackalem/ui`, запуск приложения — в `hackalem/services`,
  граница будущего независимого расчётного ядра — в `hackalem/domain`.
- Настройки `HACKALEM_SOURCE_DIR` и `HACKALEM_DATA_DIR` разрешаются от корня
  проекта. По умолчанию база находится в `.local/hackalem.sqlite3`.
- SQLite создаёт только `app_metadata` и версию схемы 1. Повторный запуск
  сохраняет метаданные; чужая база, неизвестная версия или неверная структура
  отклоняются без изменения существующих данных.
- Исходники, окружение и рабочая база исключены из Git. README содержит
  проверенные команды Windows PowerShell и описание настройки путей.

## Выполненные проверки этапа 1

Среда: Windows, CPython 3.14.7, Streamlit 1.64.0, pandas 3.0.6, numpy 2.5.3,
openpyxl 3.1.5, Plotly 6.9.0, pytest 9.1.1. Полные версии — в `uv.lock`.

| Проверка | Фактический результат |
|---|---|
| `uv sync --locked` | Окружение создано, 45 пакетов установлены |
| `uv run --locked python -m pytest -q` | 9 passed |
| Настройка путей и защита папок поставщиков | Относительные пути привязаны к проекту; запись в исходные папки отклоняется |
| Повторная инициализация SQLite | Пользовательские метаданные сохраняются, integrity_check = ok |
| Неизвестная версия схемы | Версия 99 отвергается, содержимое базы не изменяется |
| Чужая SQLite-база | Непустые базы версий 0 и 1 отвергаются; байты файлов не изменяются |
| Streamlit AppTest | Стартовый экран и повторный запуск без исключений; при недоступной папке есть предупреждение |
| Реальный локальный сервер | Главная страница HTTP 200; `/_stcore/health` HTTP 200 |
| SHA-256 исходников до/после | Проверены 12 XLSX и 1 PDF; изменённых файлов 0 |
| `git diff --check` | Ошибок форматирования нет |
| Правила `.gitignore` | Исходные выгрузки, `.venv` и SQLite исключены |

Стартовый экран проверен программно через AppTest и запуск HTTP-сервера.
Ручная визуальная проверка в браузере в этот этап не включена. В момент
завершения этапа 1 проверки импорта и бизнес-расчётов были неприменимы.
Проверочный сервер остановлен после проверки; приложение запускается командами ниже.

## Результат этапа 2

- Перед продолжением повторно выполнены все 9 проверок этапа 1: прошли.
  Необходимости переделывать каркас не обнаружено.
- Импортированы шесть файлов Systeme Electric, восемь листов, включая два
  скрытых. Проверены реальные заголовки; неверный XML dimension динамики не
  обрезает строки. Исходники читаются без пересохранения и пересчёта формул.
- Схема SQLite обновлена с 1 до 2 добавочной миграцией. Технические метаданные
  сохраняются. Добавлены версии файлов, листы, исходные строки/ячейки, товары,
  транзакции, месячные наблюдения, показатели, сезонные агрегаты и замечания.
- Каждый факт сохраняет файл, лист, физическую строку и ячейку. Формулы и
  результаты Excel сохранены отдельно; пропуск, ноль и ошибка различаются.
  Повторяющиеся строки продаж не удаляются по совпадению бизнес-полей.
- Снимок выбирает ровно одну версию каждого из шести источников. Хранятся
  SHA-256, имя, пути, время импорта, заявленная дата среза, версия правил
  `systeme-1`, хэш парсера, кода/зависимостей и JSON параметров с его хэшем.
  Полная дата 22.09.2026 взята только из имени текущего среза; ETA «24.09»
  остаётся без года. Расчётного ядра пока нет, это явно указано в параметрах.
- Новая загрузка атомарна: неизвестная структура или неполный/неоднозначный
  состав отменяет весь новый набор. Ошибки отдельных ячеек фиксируются без
  потери независимых корректных строк. Все старые снимки доступны по ID.
- Добавлены минимальный экран импорта/отчёта/происхождения и четыре команды
  CLI. Полный интерфейс закупщика этапа 10 не реализовывался.
- В рабочей `.local/hackalem.sqlite3` сохранён **снимок №1**. Повторный CLI-импорт
  переиспользовал все шесть версий и тот же снимок, количество фактов не выросло.

### Объёмы исходного набора

| Источник | SKU | Нормализованные наблюдения |
|---|---:|---|
| Кратности | 554 | 554 кратности |
| Динамика | 565 | 77 312 строк документов |
| Месячные остатки | 701 | 23 133 месяца × SKU, включая пустые |
| Месячные продажи | 554 | 18 282 месяца × SKU, включая пустые |
| Текущий срез TDSheet | 497 | 16 401 историческое месячное значение и показатели текущего среза |
| Сезонность и две скрытые копии | без SKU | 108 исходных месячных агрегатов |

Всего: 79 685 исходных строк, 2 871 наблюдение справочника по источникам,
77 312 транзакций, 57 816 месячных наблюдений, 11 602 показателя,
108 сезонных наблюдений. Наблюдения справочника не означают такое же число
уникальных товаров во всех источниках. Контрольные объёмы не зашиты в импортёр.

Отчёт: **0 ошибок ячеек/чтения, 993 предупреждения**. В их числе 943
отрицательных количества в трёх разных представлениях продаж, 13 пустых
количеств документов и 26 нулевых кратностей месячного отчёта. Остальные
предупреждения описывают неполную раннюю историю, отсутствие client_id,
неизвестные единицы сезонности, категории, смысл «СС реал», MOQ, ETA и AP/AQ.
Это счётчик замечаний к источникам, а не число уникальных проблемных SKU.
Исходные отрицательные значения и нулевая кратность не исправлялись молча.

### Выполненные проверки этапа 2

| Проверка | Фактический результат |
|---|---|
| `uv run --locked python -m pytest -q` | **27 passed in 27.29s**, включая реальный набор |
| Переход схемы 1 → 2 | Метаданные пользователя сохранены, повторный запуск безопасен |
| Неизвестная/повреждённая схема | Отказ без изменения существующей базы |
| Семь независимых Excel fixtures | Заголовки, ложный dimension, скрытый лист, формулы/cache, строковые коды, допустимые дубликаты, negative/blank/zero/error |
| Повторный импорт реальных файлов | Тот же ID снимка, шесть версий переиспользованы, факты не удвоены |
| Изменённая временная копия MOQ | Только один новый файл; старое E3=1 и новое E3=2 доступны через разные снимки |
| Версии имени/правил/кода/параметров | Контекст даты имени не теряется; старые снимки сохраняют свои версии |
| Ошибочная новая загрузка | Полный откат, старый снимок сохранён, временная папка убрана |
| Происхождение на реальных файлах | AP3=3773 и `=SUM(AC3:AO3)`, AX3=1118, BC484=37800, пустая H25635, скрытая B4 и общий заголовок ETA BC2 |
| SQLite реального проверочного набора | `integrity_check=ok`, `foreign_key_check` пуст |
| Streamlit AppTest | Импорт, повторная загрузка, выбор снимка и происхождение работают; отдельно проверен рабочий реальный снимок №1 |
| Реальный локальный сервер | Главная страница и `/_stcore/health`: HTTP 200; проверочный сервер остановлен |
| SHA-256 исходников | Все 12 XLSX и 1 PDF совпадают с контрольными суммами до этапа 1; изменений 0 |
| `git diff --check` | Ошибок форматирования нет |

Проверки изменения файлов использовали только временные копии. Исходные
PDF/XLSX, пользовательские материалы и существующие настройки сохранены.
Ручная визуальная проверка браузером не выполнялась; UI проверен через AppTest.

## Результат этапа 3

- Перед продолжением повторно выполнен полный набор этапа 2: **27 passed in
  24.93s**. Исправлять предыдущий этап не потребовалось; его исходники, данные
  и работающий интерфейс сохранены.
- Добавлен парсер всех шести книг IEK с распознаванием заголовков, сохранением
  формул, кэшированных значений, исходных строк и признаков скрытия. Месячные
  остатки имеют тип `opening_stock`, итог хранится отдельно. Это не текущий запас.
- Шесть колонок пути развернуты в строки с номером заказа, датой заказа,
  количеством и `eta_deadline`. Крайний обещанный срок не объявляется фактом
  приёмки. Полные заголовки, включая РФ/ПП, сохранены в исходных ячейках;
  нормализованный номер имеет вид `УТ-7583`.
- MOQ сохраняет 1 938 фактов и 1 937 SKU; повторный код не удалён из фактов.
  Все 15 `#N/A` сохранены как ошибки со ссылкой на SKU/ячейку, без подстановки 1.
  Служебные строки пути с пустыми кодами или 0/1 отмечены отдельно.
- Из MOQ извлечён встроенный кэш прайса от **03.08.2026**: 39 275 артикулов,
  категории, группы, статусы, единицы, тара, кратность, минимум и базовая цена
  в KZT. Внешний файл/URL не открывается, связи не обновляются. Три внешних
  псевдолиста сохранены с маркировкой `archived_external_cache`; два из них пусты.
- Общий API `hackalem.services.imports` обслуживает обоих поставщиков.
  Снимок связан с одним поставщиком и точными версиями шести источников.
  SQLite дополнена схемой 3 с сохранением старых таблиц, метаданных и снимков.
- Учётная и закупочная единицы, MOQ, кратность и предложенный коэффициент
  сохраняются раздельно с доказательствами. Подтверждение коэффициента требует
  автора, основания и явного согласия с применимостью единиц; действует только
  для конкретного снимка/SKU. Оно не подтверждает архивные MOQ или цены.
- Интерфейс дополнен выбором поставщика, архивными источниками, диагностикой
  единиц конкретного SKU и подтверждением перевода. Добавлены CLI `import-iek`
  и `units`; прежние команды продолжают работать.
- В рабочей базе сохранён **снимок IEK №2**; **Systeme №1** доступен без потери
  данных. Повторная загрузка IEK переиспользует все шесть файлов и снимок №2.
  В рабочей базе не создавались подтверждения единиц: пример подтверждён только
  в изолированной тестовой базе.

### Объёмы и замечания IEK

| Источник | Контрольный результат |
|---|---|
| MOQ | 1 938 минимумов, 1 937 SKU, 15 ошибок |
| Динамика | 171 603 строки, 2 151 SKU, 18 пустых количеств, 115 отрицательных |
| Начальные месячные остатки | 2 853 SKU, 94 149 месячных наблюдений |
| Месячные продажи | 2 463 SKU, 81 279 месячных наблюдений |
| Путь | 306 строк заказов, 300 SKU с положительными поступлениями; 20 служебных строк |
| Сезонность | 36 месячных наблюдений; сохранены 19 скрытых строк листа |
| Архивный прайс | 39 275 артикулов, дата 2026-08-03, три внешних псевдолиста |

Итого сохранены 220 825 исходных строк, 12 018 наблюдений справочников,
171 603 транзакции, 175 428 месячных наблюдений, 7 254 показателя,
306 поступлений, 39 275 архивных записей и 36 сезонных наблюдений.
Шесть листов исходных книг и три внешних псевдолиста считаются раздельно.
Контрольные объёмы не являются жёсткими ограничениями импортёра.

Отчёт содержит **46 ошибок исходных ячеек**: 15 `#N/A` в MOQ и 31 ошибку NTIN
в архивном прайсе. Это сохранённые проблемы данных, а не сбой загрузки. Также
есть **483 предупреждения**, включая отрицательные значения, пропуски,
служебные/повторные строки, примечания единиц и неподтверждённую актуальность.
Ошибки одних ячеек не отменяют корректные товары.

Отдельная диагностика единиц охватывает объединение 3 183 SKU: у 204 единицы
различаются, у 330 отсутствует учётная единица, у 617 нет однозначной пары
в архивном каталоге. У 2 032 совпадают подписи единиц, но применимость архивного
источника всё равно не подтверждена. Эти статусы не прибавлены к счётчику
предупреждений ячеек и не означают готовности к закупке.

### Выполненные проверки этапа 3

| Проверка | Фактический результат |
|---|---|
| Полный набор после добавления IEK | **37 passed in 92.73s**, включая обе реальные папки |
| После последнего изменения UI и добавления теста подтверждения | **14 passed in 2.94s**: весь затронутый UI, подтверждения, миграция и версии; всего 38 разных тестов выполнены успешно |
| Семь независимых IEK fixtures | MOQ `#N/A`, дубли фактов, начальные остатки и итог, скрытые строки, пустой XML `t=e`, заголовки пути, 0/1, кэш архива с неизвестной датой/валютой |
| Миграция SQLite 2 → 3 | Прежний снимок, факты, ID, хэши и пользовательские метаданные сохранены; повторная миграция безопасна |
| Приёмка реального IEK | Все приведённые объёмы, шесть номеров/дат заказов и 300 SKU поступлений совпали |
| Происхождение | Путь F471=5 и заголовок F1, архив P8146=662 KZT, дата прайса, минимум/кратность, скрытая F28 |
| Подтверждение перевода | 280200087_: предложение 4; без подтверждения отказ; после подтверждения 5 компл × 4 = 20 шт |
| Область подтверждения | Не применяется к другому SKU/снимку; нулевой/отрицательный/нечисловой коэффициент и отсутствие автора/основания отклоняются |
| Повторный реальный CLI-импорт | Все 6 файлов переиспользованы, снимок №2 и количества не изменились |
| Рабочая база после миграции/импорта | 12 версий файлов, старый Systeme №1 сохранён, `integrity_check=ok`, `foreign_key_check` пуст |
| AppTest на рабочей базе | Оба реальных снимка открываются; архивная ячейка и предложение коэффициента доступны; подтверждений в рабочей базе 0 |
| Реальный сервер | Главная страница и health: HTTP 200; проверочный сервер остановлен |
| SHA-256 исходников | Все 12 XLSX и 1 PDF совпадают с исходными контрольными суммами; изменений 0 |
| `git diff --check` | Ошибок форматирования нет |

Подтверждение и изменение тестовых файлов выполнялись только во временных
папках. Источники не отправлялись во внешние сервисы. Ручной визуальный осмотр
браузера не выполнялся; проверка UI проведена через AppTest.

## Запуск

```powershell
Set-Location 'C:\Users\bozta\Desktop\hackalem\hack-3b47610b-blank'
uv sync --locked
uv run --locked python -m streamlit run app.py
```

Адрес: `http://127.0.0.1:8501`. Установленное окружение также запускается командой
`.\.venv\Scripts\python.exe -m streamlit run app.py`.

В интерфейсе выберите снимок №1 (Systeme) или №2 (IEK). Для загрузки выберите
поставщика и нажмите «Импортировать …».
Отчёт и происхождение без браузера:

```powershell
uv run --locked python -m hackalem import-systeme
uv run --locked python -m hackalem import-iek
uv run --locked python -m hackalem report --snapshot 1
uv run --locked python -m hackalem trace --snapshot 1 --source current --sheet TDSheet --cell AP3
uv run --locked python -m hackalem units --snapshot 2 --sku 280200087_
```

## Передача следующему этапу и ограничения

Следующий незавершённый этап — **4: согласовать источники и реализовать проверки
качества**. Он не начат. Контракты импортов описаны в `docs/IMPORT_SYSTEME.md`
и `docs/IMPORT_IEK.md`; общий API — `hackalem.services.imports`.

Реальные данные обоих поставщиков загружены. Согласование альтернативных
отчётов, актуальные бизнес-условия, синтетический проверочный бизнес-набор,
обработка возвратов/разовых продаж, восстановление спроса, прогноз, расчёт
заказа, утверждение и экспорт не реализованы. Fixtures импорта не заменяют
набор этапа 5. Подтверждение единиц не является утверждением заказа.

Исходные замечания сохраняются: нельзя использовать AP/AQ как готовый прогноз,
считать BB Systeme существующим заказом, повторно вычитать резерв из AZ,
назначать год частичной ETA или считать начальный месячный остаток IEK
текущим. Архивные цены/MOQ не активированы; актуальные условия и подтверждённые
единицы пути необходимо согласовать до реального расчёта закупок.
